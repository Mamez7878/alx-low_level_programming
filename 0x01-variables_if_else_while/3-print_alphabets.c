#include <stdio.h>

/**
 * main - main function
 *
 * Return: 0
 */

int main(void)
{
	int i, j;

	for (i = 97; i <= 122; i++)
		putchar(i + 48);
	for (i = 65; i <= 90; i++)
		putchar(i + 48);
	return (0);
}
